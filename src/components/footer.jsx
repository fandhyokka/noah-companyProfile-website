import images from "../images";
import useMediaQuery from "../hooks/useMediaQuery";

const Footer = () => {
  return (
    <footer className="bg-blue" style={{ width: "1728px", height: "200px" }}>
      <div></div>
    </footer>
  );
};

export default Footer;
